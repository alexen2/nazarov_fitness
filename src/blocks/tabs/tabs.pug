mixin tabs(data)
	ul.tabs(class!=attributes.class)
		- for (var i = 0; i < data.length; i++)
			li.tabs__item
				if i + 2 < data.length
					a.tabs__link.tabs__link--active(href=data[i]['url'])!=data[i]['link']
				else
					a.tabs__link(href=data[i]['url'])!=data[i]['link']